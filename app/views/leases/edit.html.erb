<h1>Editing Lease</h1>

<%= render 'form', lease: @lease %>

<%= link_to 'Show', [@current_user, @property, @lease] %> |
<%= link_to 'Back', user_property_leases_path(@current_user, @property, @lease) %>
