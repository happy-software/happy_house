<%= form_with(model: @lease, url: [@property, @lease]) do |form| %>
  <% if lease.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lease.errors.count, "error") %> prohibited this lease from being saved:</h2>

      <ul>
      <% lease.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_date %>
    <%= form.datetime_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :end_date %>
    <%= form.datetime_select :end_date %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.number_field :amount %>
    <%= form.collection_select :lease_frequency_id, LeaseFrequency.order(:frequency), :id, :frequency, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :contract %>
    <%= form.file_field :contract %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
